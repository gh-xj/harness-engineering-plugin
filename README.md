# harness-engineering-plugin

Bootstrap harness plugin with a superpowers-compatible harness profile.

## Usage

Run the CI task:

```bash
task ci
```

Expected smoke artifact:

- `artifacts/harness/smoke.json`
